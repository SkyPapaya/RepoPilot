# RepoPilot

RepoPilot is a learning-oriented coding agent project.

Current status:

- Python project structure
- CLI
- uv dependency management
- pytest smoke test

Run:

```bash
uv sync
uv run repopilot
```

Test:

```bash
uv run pytest
```
